CREATE TABLE blop (
  id INTEGER PRIMARY KEY NOT NULL,
  title TEXT NOT NULL,
  color INTEGER NOT NULL,
  sessionId INTEGER NOT NULL
);

getAll:
SELECT * FROM blop WHERE sessionId = :sessionId;

insert:
INSERT OR REPLACE INTO blop VALUES (?,?,?,?);

delete:
DELETE FROM blop WHERE id = :id;