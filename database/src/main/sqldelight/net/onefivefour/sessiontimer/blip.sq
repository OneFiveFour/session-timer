CREATE TABLE blip (
  id INTEGER PRIMARY KEY NOT NULL,
  title TEXT NOT NULL,
  blopId INTEGER NOT NULL
);

getAll:
SELECT * FROM blip WHERE blopId = :blopId;

insert:
INSERT OR REPLACE INTO blip VALUES (?,?,?);

delete:
DELETE FROM blip WHERE id = :id;